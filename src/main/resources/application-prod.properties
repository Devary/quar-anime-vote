quarkus.vault.url=${VAULT_URL}
quarkus.vault.secret-config-kv-path=@vault.secret.config.path@
quarkus.vault.kv-secret-engine-mount-path=@vault.kv.mount.path@
quarkus.vault.secret-config-kv-path.db=@vault.secret.config.path@
quarkus.vault.authentication.kubernetes.role=@vault.kubernetes.role@

quarkus.datasource.db-kind=${db.kind:postgresql}
quarkus.datasource.username=${db.username}
quarkus.datasource.password=${db.password}
quarkus.datasource.reactive.url=${db.reactive-url}
quarkus.datasource.reactive.max-size=${db.reactive-max-size:20}
quarkus.hibernate-orm.database.generation=${db.hibernate-generation:update}
quarkus.datasource.jdbc.url=${db.jdbc-url}

mp.jwt.verify.issuer=${JWT_ISSUER:http://192.168.178.41:8081/realms/dev}
mp.jwt.verify.publickey.location=${JWT_ISSUER:http://192.168.178.41:8081/realms/dev}/protocol/openid-connect/certs
smallrye.jwt.path.groups=realm_access/roles
anime.keycloak.token-url=${KEYCLOAK_TOKEN_URL}
anime.keycloak.client-id=${KEYCLOAK_CLIENT_ID:anime-vote}
quarkus.http.auth.proactive=${GATEWAY_AUTH_ENABLED:false}

# LDAP (values come from Vault kv/anime-vote)
anime.ldap.url=${ldap.url:ldap://fedora.fritz.box:389}
anime.ldap.base-dn=${ldap.base-dn:cn=dev,dc=local}
anime.ldap.bind-dn=${ldap.bind-dn:cn=Directory Manager}
anime.ldap.bind-password=${ldap.bind-password}
anime.ldap.people-ou=${ldap.people-ou:people}
